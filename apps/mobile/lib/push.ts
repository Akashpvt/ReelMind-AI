import Constants from"expo-constants";import*as Device from"expo-device";import*as Notifications from"expo-notifications";import{Platform}from"react-native";import{supabase}from"@/lib/supabase";
Notifications.setNotificationHandler({handleNotification:async()=>({shouldPlaySound:true,shouldSetBadge:true,shouldShowBanner:true,shouldShowList:true})});
export async function registerPushToken(organizationId?:string){if(!Device.isDevice)return null;const existing=await Notifications.getPermissionsAsync();let status=existing.status;if(status!=="granted")status=(await Notifications.requestPermissionsAsync()).status;if(status!=="granted")return null;if(Platform.OS==="android")await Notifications.setNotificationChannelAsync("default",{name:"ReelMind",importance:Notifications.AndroidImportance.HIGH});const projectId=process.env.EXPO_PUBLIC_EAS_PROJECT_ID||Constants.expoConfig?.extra?.eas?.projectId;const token=(await Notifications.getExpoPushTokenAsync(projectId?{projectId}:undefined)).data;const{data:{user}}=await supabase.auth.getUser();if(user)await supabase.from("mobile_push_tokens").upsert({user_id:user.id,organization_id:organizationId||null,expo_push_token:token,platform:Platform.OS,device_name:Device.deviceName,enabled:true,updated_at:new Date().toISOString()},{onConflict:"expo_push_token"});return token}
