import{PublishingHub}from"@/components/team/publishing-hub";import{TeamShell}from"@/components/team/team-shell";import{requirePagePermission}from"@/lib/team/permission-guards";
export const dynamic="force-dynamic";export default async function PublishingPage(){const access=await requirePagePermission("content:edit","/dashboard/team/publishing");return <TeamShell title="Social Publishing Hub" subtitle="Create once, distribute intentionally, and keep every account, schedule, result, and next idea in one place."><PublishingHub organizationId={access.organizationId}/></TeamShell>}
